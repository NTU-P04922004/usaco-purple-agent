[project]
name = "usaco-purple-agent"
version = "0.1.0"
description = "USACO Benchmark Purple Agent"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "a2a-sdk[http-server]==0.3.20",
    "loguru==0.7.3",
    "openai==1.109.1",
    "uvicorn==0.38.0"
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "httpx>=0.28.1"
]
